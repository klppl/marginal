<!DOCTYPE html>
<html lang="{{@site.locale}}" class="{{#match @custom.color_palette "Catppuccin Mocha"}}palette-catppuccin-mocha{{else match @custom.color_palette "Dracula"}}palette-dracula{{else match @custom.color_palette "Nord"}}palette-nord{{else match @custom.color_palette "Gruvbox"}}palette-gruvbox{{else match @custom.color_palette "Tokyo Night"}}palette-tokyo-night{{else match @custom.color_palette "Rose Pine"}}palette-rose-pine{{else match @custom.color_palette "Everforest"}}palette-everforest{{/match}}">
<head>

    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="color-scheme" content="light dark">
    <link rel="stylesheet" href="{{asset "built/index.css"}}" />

    <title>{{meta_title}}</title>

    <style>
        :root {
            --global-font: {{#match @custom.body_font "System Serif"}}Georgia, "Times New Roman", serif{{else match @custom.body_font "Monospace"}}ui-monospace, Menlo, Monaco, "Cascadia Code", monospace{{else}}-apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Oxygen, Ubuntu, Cantarell, "Fira Sans", "Droid Sans", "Helvetica Neue", sans-serif{{/match}};
        }
    </style>

    {{ghost_head}}

</head>

<body class="{{body_class}}">

<header class="gh-head">
    <div class="gh-head-brand">
        <a class="gh-head-logo" href="{{@site.url}}">
            {{#if @site.logo}}
                <img src="{{@site.logo}}" alt="{{@site.title}}" />
            {{else}}
                {{@site.title}}
            {{/if}}
        </a>
    </div>
</header>

<main class="gh-main" style="padding: 2rem 1.5rem;">

    <p><strong>{{statusCode}}</strong></p>
    <h1>{{message}}</h1>
    <p><a href="{{@site.url}}">{{t "Go to the front page"}} &rarr;</a></p>

    {{#if errorDetails}}
    <section class="gh-error-stack">
        <h4>Theme errors:</h4>
        <ul>
            {{#foreach errorDetails}}
            <li>
                <h5>{{{rule}}}</h5>
                {{#foreach failures}}
                <span><strong>Ref:</strong> {{ref}}</span><br>
                <span><strong>Message:</strong> {{message}}</span>
                {{/foreach}}
            </li>
            {{/foreach}}
        </ul>
    </section>
    {{/if}}

</main>

<footer class="gh-foot">
    <nav class="gh-foot-menu">
        {{navigation type="secondary"}}
    </nav>
    <div class="gh-foot-meta">
        {{t "published with"}} <a href="https://ghost.org" target="_blank" rel="noopener">Ghost</a>
    </div>
</footer>

{{ghost_foot}}

</body>
</html>
